export const environment = {
  production: false,
  // Matches the "Talamis" profile in backend/Talamis/Properties/launchSettings.json.
  // Update this if your backend runs on a different port.
  apiUrl: 'https://localhost:64387/api',
};
